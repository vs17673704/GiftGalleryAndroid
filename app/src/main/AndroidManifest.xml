<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.nasr">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/nasr_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ValidationActivity"></activity>
        <activity android:name=".list.CustomerList" />
        <activity android:name=".AdminDashboard" />
        <activity android:name=".RegisterCustomerProfile" />
        <activity android:name=".CustomerDashboard" />
        <activity android:name=".EditCustomerProfile" />
        <activity android:name=".list.OrderList" />
        <activity android:name=".list.ItemList" />
        <activity android:name=".list.CustomerItemList" />
        <activity android:name=".list.CustomerOrderList" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>